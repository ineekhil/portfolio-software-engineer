# Copy this file to `.env.local` and adjust values.
# Next.js loads `.env.local` automatically (see https://nextjs.org/docs/app/api-reference/config/env)

# Public variables (exposed to the browser — must be prefixed with NEXT_PUBLIC_)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
